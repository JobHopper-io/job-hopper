## Icons

- **Use Font Awesome for all standard icons.**
  - Icons should be rendered via the global `font-awesome-icon` component configured in `src/plugins/fontawesome.ts`.
  - When you need a new icon, add it to the shared Font Awesome plugin (import the icon there, add it to the `library`, and use it by name in templates).

